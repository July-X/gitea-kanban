# IDE / agent
.codex/
.harness/
.mavis/
.opencode/
.claude/

# 构建产物
node_modules/
out/
dist/
coverage/

# 临时/缓存
*.log
.DS_Store
.tmp/
.cache/

# 时间轴视觉调参过程截图（M6 调参草稿，不入仓）
notes/cap_*.png

# e2e bundle 产物（worker esbuild 自动生成，跑完可重建）
scripts/.e2e-verify-*.bundled.mjs

# gitea 测试服务运行时数据
giteaDemo/data/

# 渲染端类型生成物（d.ts 编译产物）
src/renderer/.d.ts/

# drizzle studio local
drizzle/.snapshot/

# 调研用第三方 API 文档（gitea swagger yaml，不入仓）
docs/plugin-redoc-2.yaml
.codegraph
.reasonix
reasonix.toml
