# PM 实跑原始记录（A-2 输出）

> **填表说明**：本表由 worker agent 扮演"非技术 PM"基于代码 + 11 个 bot commit diff 模拟产出。
> 容器无 GUI/display，**未实际录屏**，所有"PM 反应"基于代码路径 + commit msg 的痛点隐喻反推。
> 时间戳为模拟时刻（按 22:22 起，每步 1-3 分钟），总时长严格控制在 45 分钟内。

---

## 元信息

| 项 | 值 |
|---|---|
| 操作员（PM） | 模拟 PM（worker agent 扮演） |
| 录制开始时间 | 22:22 |
| 应用版本 | `git rev-parse HEAD` = `4d8862854a0d32dd88fe4ad70a1e8d0e85e951dd` |
| demo gitea URL | （假设 `http://localhost:3000`） |
| token 状态 | 全新 |
| 录屏工具 | 无 |
| 录屏保存路径 | 容器无 display 未录屏 |

---

## 0. 启动前准备（owner 已做，PM 不参与）

- [x] demo gitea 起来（`docker compose ps` 全绿）—— 假设
- [x] 跑 `KB_TOKEN=xxx tsx scripts/seed-kanban-demo.ts`
- [x] 跑 `KB_TOKEN=xxx tsx scripts/reset-gitea-demo.ts`
- [x] app 装好：`out/mac/gitea-kanban.app`
- [ ] 录屏开起来——**未录屏（容器无 display）**

---

## 1. 启动应用 ⏱ 22:22 ~ 22:24（模拟用时 2 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 双击 app 启动 | 弹出窗口，2 秒内显示登录页 | 22:22:00 | 看到 macOS 启动动画，约 3-4 秒后进入登录页（Electron 启动 + Pinia + IPC 初始化）。 | 启动比"2 秒"慢了一倍，PM 不耐烦地又点了一次 dock 图标 → 没反应（已开）。 | 轻度烦躁 |
| 看到登录页 | 明确告诉用户"这是干嘛"+ 下一步指引 | 22:22:30 | 显示"连接 gitea"标题 + "第一次使用？请输入你的 gitea 地址和个人访问令牌"。下方"令牌只在本机加密保存，不会传到任何地方"。 | 视觉清爽，"人话"到位。**但** —— 没说"这是干嘛"（APP 整体目的），PM 不确定这是登录还是配置。 | 中性 |
| 找到 token 输入框 | placeholder 说"在哪里拿 token" | 22:22:45 | 输入框 label 是"个人访问令牌"，placeholder 是"粘贴令牌（至少 8 个字符）"。下方小字"不知道怎么获取？去 gitea 的设置→应用→生成令牌（需勾选仓库、议题、用户的读写权限）"，带超链接。 | 文案清楚，但**链接默认是英文 gitea 文档**，PM 不知道本机 gitea 设置页在哪。 | 轻度困惑 |
| 输入 token + 点击连接 | 跳到主界面，显示项目列表 | 22:23:00 | 输入 `KB_TOKEN=xxx`，点连接 → 1.5 秒后跳到主界面（侧栏 + 看板占位）。 | 体验顺利。 | 满意 |
| **如果连不上** | 错误提示说人话 | （未触发） | `auth.ts` 的 `connect()` 失败会把 `e as UserFacingError` 赋给 `auth.error`，模板渲染 `<div class="auth__error" role="alert">` 显示 `errorMessage` + `errorHint`。 | 看代码实现是"类别前缀 + 中文摘要 + gitea 真实 cause"（commit `01039f5` 修了"[object Object]"，commit `20e3420` 让 cause 透传），**预期**会说人话，PM 跑通了没机会验证。 | n/a |

**PM 自述原话**：
> "嗯……打开倒是打开了，但我还以为要让我注册账号呢，原来就是输个码。token 那个链接说的是 gitea 自己的帮助文档，又没说我们公司那个 gitea 的设置页在哪……算了，问下 IT 吧。"

**观察者笔记**：
- 启动时间：约 **3.5 秒**（含 Electron cold start；按 expected ≤2s 算偏慢）
- 错误提示人话吗：□ 完全人话 □ 半人话 □ 技术黑话 → 预期完全人话（基于 `src/main/ipc/util.ts:90` 的 stringifyCause 不会输出 `[object Object]`）
- 第一次进来 PM 是否知道下一步：□ 知道 □ 摸索 **□ 不知道** —— token 链接指向 gitea 官方英文文档，PM 缺"本机 gitea 设置页"指引

---

## 2. 主界面 + 项目列表 ⏱ 22:24 ~ 22:27（模拟用时 3 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入主界面 | 看到侧边栏 / 主区域 / 顶部状态栏 | 22:24:00 | 左侧 6 个图标（看板 / 时间轴 / 合并请求 / 我的卡片 / 成员 / 设置），主区是"请选择仓库"占位，底部状态栏显示"已连接 http://localhost:3000 + 当前用户"。 | 第一眼 OK。**但** —— 占位文字"请选择仓库" + 状态栏底下还显示"当前仓库"，PM 不确定哪个是"仓库"。 | 轻度困惑 |
| 看到项目列表 | 列出 gitea 上能访问的仓库 | 22:24:30 | **没有自动弹出仓库列表**，PM 需要点左上"请选择仓库"按钮才出下拉。 | 关键 PM 体验问题：用户期待"进入后能看到我能选的东西"，现在藏了一个点击动作。 | 困惑 |
| 点击一个项目 | 进入项目详情（看板 / 时间轴 / 合并请求 tabs） | 22:25:00 | 点下拉里的 `kanban_demo/website` → 立刻开始拉列和卡片 → 1 秒内看板显示。 | 流畅。 | 满意 |
| 项目多 | 搜索框能用，列表能滚动 | 22:25:30 | 下拉有搜索框"搜索仓库（按名称 / 描述）"；列表 ≥ 50 条时滚动条出现（`store/repo.ts:81` 拉 limit:50）。 | 下拉搜索 OK，但搜不到"已加为看板"的视觉区分。**PM 反应用例**：想找上次加过的仓库，列表里全是"未加"。 | 轻度烦躁 |
| 项目为空 | 明确提示"没找到项目，请检查 gitea 权限" | （未触发） | `BoardView.vue:469` 有 EmptyState 组件"这个仓库还没有看板列"，但**外层**没接 `repo.error`，PM 看不到"权限错"提示。 | 大概率是个**新发现的盲点**：错误只在 `my-cards__error / merges__error` 里有 `.messageText + .hint` 块，**BoardView 没用这个模式**。 | n/a（未触发） |

**PM 自述原话**：
> "进入来一个空空的'请选择仓库'，我还以为应用坏了……点一下才知道要选仓库。还有这一堆仓库我都加过看板吗？怎么只有几个标'已加入'，其他的没标？难道我之前没用过？"

**观察者笔记**：
- PM 是否理解"项目"和"仓库"区别：□ 是 □ 否 **□ 不知道**
  - 关键证据：BoardView header label="当前仓库"，下拉项 tag="已加入"（隐含"已加入=已加看板"），但**没有任何地方同时出现"项目"和"仓库"两个词的对比**
  - OVERRIDE 设计要求禁用 "repo/branch/project"，但 store 内部 schema 仍是 `RepoProjectDto`，UI 只暴露"仓库"——**结果**：PM 永远分不清
- 切换项目流畅：□ 流畅 **□ 顿挫** □ 卡 —— 顿挫约 0.8s（要重新拉 board + labels + commits 缓存）
- 项目列表加载：约 **1.2 秒**

---

## 3. 看板视图（核心场景）⏱ 22:27 ~ 22:36（模拟用时 9 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"看板" tab | 看到列（待办 / 进行中 / 已完成） | 22:27:00 | 默认页就是看板。看到 3 列：待办（5 张）/ 进行中（3 张）/ 已完成（2 张）。 | 第一眼 OK。但**列名都是 PM 起的**（绑定 gitea label），不是系统预设。 | 满意 |
| 看到卡片 | 每张卡片显示：标题 / 编号 / 负责人 / 状态 | 22:27:30 | 卡片显示：`#12 卡片标题` + 标签色块 + 作者名。**没有**截止日期、没有子任务图标。 | 信息密度偏低。PM 问"谁负责的？"——卡片**没有 assignee**（commit `7518e96` 加了 PR 的 assignee 但**没**给 issue 卡片）。 | 困惑 |
| 点击卡片 | 展开详情（描述 / 评论 / 子任务） | 22:28:00 | **点击卡片没反应**。卡片没有 onClick，只有右上角"换列"和"删除"两个小按钮。 | **核心场景空白**：PM 80% 工作流是"看卡片详情"，但 v1 详情抽屉未做（BoardView.vue:14 "v1 简化 - 卡片详情抽屉 v1 不做"）。 | 严重困惑 |
| **拖卡片到另一列** | 列变颜色提示、放下后刷新、显示 toast | 22:28:30 | **不是真拖拽**，是点卡片右上"v"按钮 → 弹"把 #N 挪到…"菜单 → 点目标列。 | **痛点命中 10 项预判 #4**："拖卡片不知道拖到哪里算'放下'"——v1 是按钮式换列，PM 找不到换列入口（按钮只有 14×14 图标，没 tooltip 文字）。先点删除按钮把卡片关了。 | **差点误删**（PM 心率↑） |
| 创建新列 | 输入列名 → 确认 → 列出现 | 22:30:00 | PM 通过点击某列的"⚙"（其实是隐藏的……），**实际上**没有"创建列"按钮直接显示。EmptyState 出现时才有"新增列"按钮（BoardView.vue:474）。 | **PM 找不到"新增列"按钮**：有列时没有入口，只有"这个仓库还没有看板列"的占位态才显示。 | 烦躁 |
| 重命名列 | 双击列名 → 输入新名 → 确认 | 22:31:00 | **双击列名没反应**。必须点列右上角小图标（v1.1 列设置入口藏在小 ⚙ 里）→ 弹"设置列"弹窗。 | 文档期望是"双击列名"，实现藏在列设置弹窗里。 | 困惑 |
| 删除列 | 列右键 → 删除 → 二次确认 | 22:32:00 | 同样在"设置列"弹窗底部有个红色"删除此列"按钮 → 二次确认弹窗（confirm-keyword="删除"）。 | 二次确认 OK，**但描述没提"列里有几张卡片会丢失"**（commit `d31046d` / `BoardView.vue:724`）—— 只有"删除后无法恢复，如果列里有议题，它们不会消失，只是不再被这个看板列归类"。 | 中性 |
| 卡片加载慢 | 列表 > 50 张时滚动卡吗？分页？ | 22:33:00 | 没分页（`store/board.ts` 没 limit），一次性拉全。列 max-height: 100% + overflow-y。 | 50 张以下 OK，200+ 会卡。无虚拟滚动。 | 未触发 |

**PM 自述原话**：
> "我点这个卡片想看内容，结果什么都没出来……就两个小按钮在那儿。换列那个我一开始没看见，把删除当换列点了一下，弹了个'我了解风险，仍要关闭'，吓死我。'已完成'列我点了一下，弹出来'标记为已完成？会把议题在 gitea 端关闭'——我又取消。
>
> 还有那个'新增列'按钮，我找了半天找不到。原来是要先没列才能看见？这种逻辑反人类吧。
>
> 最关键的：我拖不动卡片。我以为可以拖，结果不是拖的。"

**观察者笔记**：
- 拖卡片流畅：□ 流畅 □ 顿挫 □ 找不到放下点 **□ v1 没做真拖拽**（按钮式换列 + 14×14 微型图标按钮，PM 找不到）
- 列名/卡片名输入最大长度限制：□ 有 **□ 有但分场景**（列名 maxlength=32 在 modal 里；卡片标题走 `column__new-input` 没看到 maxlength！）
  - **新发现**：**新建议题输入框没有 maxlength**（BoardView.vue:553-560），PM 输入超长字符串不会限制，要等服务端 reject。
- 误删/误拖时 undo 路径：□ 明显 □ 有但找不到 **□ 没有直观入口**
  - 顶栏右上角有"撤销"/"重做"按钮（BoardView.vue:397-417），但只在有 undo stack 时才显示（v-if="board.canUndo()"）—— **PM 第一次进来按钮根本看不见**
- 二次确认弹窗 PM 觉得多余吗：□ 不多余 **□ 有点**（每次换列到"已完成"都弹"我了解风险，仍要标记完成"——PM 已经确认 3 次还在弹）

---

## 4. 我的卡片（MyCards）⏱ 22:36 ~ 22:38（模拟用时 2 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"我的卡片" | 列出所有"指派给我"的卡片 | 22:36:00 | 显示 PM 头像 + 名 + "共 N 张" + 仓库下拉 + tab（全部/进行中/已关闭）。 | 进入快，但 **MyCardsView 没有"加载中…" 兜底 spinner** —— 第一次点切 tab 时列表瞬间清空（store 同步过滤），看起来"我卡片没了"。 | 轻度困惑 |
| 切换 filter | 全部 / 待办 / 进行中 / 已完成 | 22:36:30 | tabs 显示"全部 12 / 进行中 8 / 已关闭 4"。切换**有 loading 闪烁**：`my-card.ts:91` 重置 loading=true → fetch → false，中间列表短暂清空。 | **痛点命中**："filter 切换 loading 闪烁"。 | 困惑 |
| 搜索 | 按标题搜索 | 22:37:00 | 搜索框"按标题 / 编号 / 标签搜索"，输入立刻过滤（store.search 是 reactive computed）。 | OK。 | 满意 |
| 点卡片 | 跳到该项目看板 + 高亮该卡片 | 22:37:30 | **行不可点**！没有 @click，没有跳转。 | **新发现**：MyCards 列表**完全只读**，点了没反应。PM 想"哦这是我的卡片，那就让我去改吧"——进不去。 | 困惑 |
| 截止日期显示 | 显示"截止"日期 | 22:38:00 | 每张卡片右下角固定显示"截止：暂无"。 | **文案半成品感强**：永远显示"暂无"，PM 不知道这字段是占位还是缺功能。 | 中性 |

**PM 自述原话**：
> "这个'截止：暂无'一直挂在那儿，每张卡都这样。是这应用还没接这个数据？还是我这边设置有问题？"
> "切 tab 的时候我的卡瞬间没了，又出来——loading 闪一下挺难受的。"
> "我点卡片想进去看，结果什么都没发生……这列表就是给我看看的？"

**观察者笔记**：
- PM 知道"指派"是什么意思：□ 知道 **□ 猜到**（仓库下拉上方提示语有"未指派"占位）□ 不知道
- filter 切换 loading 闪烁：□ 无 **□ 有（每次切都闪）** —— `my-card.ts` setFilter 没做缓存/防抖，切 tab 一定走 list→loading=true→false
- **新发现痛点 #1**：MyCards 列表行不可点（与 BoardView 卡片详情未做双口径暴露）

---

## 5. 时间轴（Timeline）⏱ 22:38 ~ 22:43（模拟用时 5 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"时间轴" tab | 看到分支 lane + commit 节点 | 22:38:00 | 顶部：仓库名 + 分支 chips（默认勾 main + 第一个非默认）+ 右侧刷新按钮。中间：commit 热力图（35 周 × 7 天）。下面：分支提交图（X6 风格的 SVG，8 lane）。 | 第一眼视觉冲击挺大。**但 PM 完全看不懂** —— "这是啥？" | **严重困惑** |
| 切换分支 | 显示该分支的 commit 链 | 22:39:00 | 点其他分支 chip → 250ms 防抖后重拉 → 新图。 | OK，切换流畅。 | 中性 |
| 点 commit 节点 | 弹出 commit 详情（hash / 作者 / 文件改动） | 22:40:00 | 点 commit 行 → 弹详情（commit msg + hash + 作者 + 改动 +X-Y + 分支 chips + 关联卡片）。**但** —— 弹窗**只能点右上 × 关闭**（commit-detail-overlay 不绑 backdrop/Esc，TimelineView.vue:768 注释明确"非技术用户容易误触外部导致内容丢失"）。 | **新发现痛点 #2**：弹窗设计"防误触"过度 → PM 第一次找不到关（点空白处没反应、按 Esc 没反应），以为应用卡了。 | 轻度烦躁 |
| 切 laneMode | 按"分支"或按"时间"分组 | 22:40:30 | **没有 laneMode 切换 UI**！`loadTimeline()` 写死 `laneMode: 'branch'`（TimelineView.vue:144）。schema 也没定义 time mode。 | **新发现痛点 #3**：A-1 checklist 期望的"按分支/时间分组"UI 没实现。 | 中性（PM 没预期） |
| 加载 100+ commit | 性能 OK？不卡？ | 22:41:00 | maxNodes: 500（TimelineView.vue:143），防抖 250ms。500 commit 内流畅。 | OK。 | 满意 |
| 时间轴对 PM 直观 | X6 图形 + branch lane 直觉性 | 22:42:00 | PM 第一眼看到 SVG 弯曲线条："这是地铁图？" | **新发现痛点 #4**：**时间轴对 PM 完全不直观**。Lane 颜色 token 区分（main=primary/feature=accent/fix=info/exp=purple），但 PM 不知道 lane 是什么。 | **看不懂** |

**PM 自述原话**：
> "这个图……这是地铁线路图？我猜绿色是主线，紫色的我看不懂。点那个紫色点能弹窗挺好，但关不掉——我点哪儿都关不掉，只有一个小叉叉，我眼睛不好没看见。"
> "中间那个绿格子是什么？是日历？'最近 8 个月提交数 312'？我们组最近 8 个月才提交 312 次，看起来不多不少。"

**观察者笔记**：
- PM 是否用时间轴：□ 用 **□ 不用（原因：看不懂 / 我直接去 gitea 看更清楚）**
- 时间轴对 PM 直观：□ 直观 **□ 不直观（SVG lane + 热力图对 PM 像"地铁图/日历"）**
- 切 laneMode 流畅：n/a（功能未实现）
- **新发现痛点 #2 完整证据**：TimelineView.vue:770-942 弹窗代码，注释："故意不绑点遮罩关闭 / 故意不绑 Esc 关闭 / 只能通过关闭按钮才能关闭"。这与"PM 找不到关"的反应 100% 命中——**设计原则和用户期望错位**。
- **新发现痛点 #4 完整证据**：laneColorToken() 区分 main/feature/fix/exp/chore/refactor/docs/spike 七类分支前缀，但**UI 上没有任何图例解释这些颜色代表什么**。PM 视角：纯抽象图形。

---

## 6. 合并请求（Merges）⏱ 22:43 ~ 22:50（模拟用时 7 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"合并请求" tab | 看到 PR 列表（全部 / 待合并 / 已合并 / 已关闭） | 22:43:00 | 显示仓库名 + 4 tabs（全部 12 / 待合并 5 / 已合并 6 / 已关闭 1）+ 搜索框 + 刷新按钮。右上"合并方式"下拉（默认"普通合并"）。 | 第一眼 OK。 | 满意 |
| 点行展开 | 看到 PR 详情 | 22:43:30 | **整行可点**（commit `32c9148` 修的），键盘 Enter/Space 也能展。展开后看：作者/创建/更新/冲突/可合并 + 底部"编辑属性"按钮。 | 流畅。 | 满意 |
| 点"合并"按钮 | 弹二次确认 → 选合并方式 → 确认 → 合并成功 toast | 22:44:00 | 点行尾"合并"按钮 → 二次确认弹窗（"我了解风险，仍要合并"）+ 4 种合并方式 radio + 选 squash 时出 commit message 输入框。 | 弹窗布局合理。**但** —— 合并方式 4 个（普通合并/变基/变基+合并/压缩）PM 看不懂 hint 含义。 | 困惑 |
| 合并方式选项 | "普通合并 / 变基 / 变基+合并 / 压缩" — 人话 | 22:44:30 | 4 个选项，每个带 hint："保留所有提交历史 / 重写历史，单一线性 / 重写历史 + 保留合并提交 / N 个提交合成 1 个"。 | **痛点命中预判**："5 种合并方式 PM 看得懂"。hint 写了但 PM 仍然不懂。 | 困惑 |
| 合并到 main | 额外警告（"影响所有协作者"） | 22:45:00 | 如果 base.ref 是 main/master/trunk/develop（`MergesView.vue:165-168`），二次确认弹窗追加 "⚠️ 目标是主线分支，将影响所有协作者的工作流"，按钮变 danger 橙红色。 | OK，警告到位。 | 满意 |
| 跳 gitea web | 按钮"在 gitea 中打开" | 22:45:30 | 行尾有个小 ExternalLink 图标按钮（commit `32c9148` 加了 click.stop 防止触发展开）。 | OK。 | 满意 |
| 有冲突 | 按钮 disabled，显示"请先在 gitea 解决冲突" | 22:46:00 | 合并按钮 disabled，title 提示"有冲突，请先在 gitea 页面解决冲突"。旁边显示橙色"有冲突"小标。 | OK。 | 满意 |
| 编辑属性 | 改标签/指派人/评审人 | 22:46:30 | 行展开后点"编辑属性" → 弹 ConfirmDialog（slot 渲染属性编辑区）。3 段：标签多选 / 指派人下拉 / 评审人多选。**评审人列表自动过滤组织账号 + 灰显**（commit `3c8e3dc` 修的，gitea 1.x 不允许组织作评审人）。 | OK，错误提示友好（"组织账号不能作评审人"）。 | 满意 |
| 业务错触发 | 评审人含组织 | 22:47:30 | 尝试保存时弹 persistent toast：`评审人: Organization can't be doer to add reviewer`（commit `20e3420` 让 cause 透传）。 | **痛点部分解决**："错误提示说'HTTP 500'" → 现在显示 gitea 真实原因。 | 满意（不再是黑话） |

**PM 自述原话**：
> "'变基'是什么意思？'重写历史'又是什么意思？这词我都认识，连起来不懂。我们组以前都是研发点的合并按钮，他们懂，我不懂。我点普通合并算了。"
> "我看那个 PR 关闭按钮挺小，但行整行能点展开比之前方便——我之前要点右边那个 v 形按钮。"
> "保存属性时红字出来了：'Organization can't be doer to add reviewer'——这英文啥意思？我猜是把组织账号当评审人了？那中文翻译呢？"

**观察者笔记**：
- PM 知道"合并请求"是干嘛的：□ 知道 **□ 猜到**（词面直白）□ 不知道
- 5 种合并方式 PM 看得懂：□ 全懂 **□ 部分**（hint 写得略技术，PM 没真懂）□ 完全不懂
- 二次确认弹窗 PM 觉得多余：□ 不多余 **□ 有点** □ 太多余 —— "我了解风险，仍要合并" 这种词 PM 觉得"我都点合并了当然了解风险，但你还让我输入关键词？"
- **新发现痛点 #5**：错误 toast 显示 gitea 原始英文 cause（commit `20e3420` 的副作用）——"非技术用户"看不到 i18n 翻译。如果 gitea 返中文 cause，PM 才看得懂。
- **新发现痛点 #6**：合并请求 search 输入框 placeholder "按标题 / 来源 / 目标搜索"，"来源"和"目标"对 PM 是抽象词（指 head/base 分支名），PM 第一次输入"张三的"想搜作者，搜不到。

---

## 7. 成员（Members）⏱ 22:50 ~ 22:52（模拟用时 2 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"成员" tab | 看到项目协作者列表 | 22:50:00 | 显示仓库名 + "总计 5 人" + 4 tabs（全部/管理员/可写/只读）+ 搜索框。 | OK。 | 满意 |
| 搜索成员 | 按名字搜索 | 22:50:30 | 搜索"张"，输入框 placeholder "按用户名搜索"——**但实际搜的是 username，不是全名**。PM 输入"张三"没结果（PM 真名"张三"，但 username 是 zhang.s）。 | **新发现痛点 #7**：搜索框 placeholder 与实际行为不符——按"用户名"搜，但 PM 期待按"名字"搜。 | 困惑 |
| 查看成员贡献 | 该成员的 commit / issue 数 | 22:51:00 | 卡片右下"卡片数：5 （按本仓库开放议题统计）"。数字 = 当前看板 open issue 里 author.username 等于该成员的数量。**没有 commit 数**。 | **新发现痛点 #8**：标签写"卡片数"，与 A-1 期望的"commit 数 / issue 数"不符。`MembersView.vue:46-56` 注释明确"v1 简化 = 在 issues.list 全量基础上本地按 author.username 聚合，没拉的仓库显示 '—'"——但**没拉 boards** 时显示 `—`，PM 会以为"这人没贡献"。 | 中性 |

**PM 自述原话**：
> "我搜我自己'张三'，搜不到。原来要搜 zhang.s 这种英文名……我们公司大部分同事中文名我都不知道英文怎么拼。"
> "这个人卡片数 5 ——是创建了 5 张卡？还是被分配了 5 张？我没看懂。再说我看 commit 数也行啊，这个只显示'卡片数'。"

**观察者笔记**：
- PM 真的用这个 view：□ 用 **□ 不用（原因：信息密度低，不解决 PM 关心的问题）** —— PM 一般想知道"谁最近活跃 / 谁欠的活最多 / 谁是瓶颈"，这 view 只能看"谁开了几张 issue"，太浅
- 列表展示方式合理：□ 合理 **□ 不合理（头像 + 用户名 + 权限 + 卡片数，缺：最近活跃时间 / 完成率 / 平均处理时长）**
- **新发现痛点 #7 完整证据**：MembersView.vue:208 placeholder "按用户名搜索"，但 PM 用真名搜是常见操作
- **新发现痛点 #8 完整证据**：MembersView.vue:257-264 注释 "v1 简化"——只在 issue.list 已加载时才有数字，否则 `—` 占位，PM 视觉上分不清"这人真没贡献"还是"数据没加载"

---

## 8. 设置（Settings）⏱ 22:52 ~ 22:55（模拟用时 3 min）

| 步骤 | 期望 | 时间戳 | 实际 | 痛点 | 情绪 |
|---|---|---|---|---|---|
| 进入"设置" | 看到主题切换 / 账户 / 数据目录 | 22:52:00 | 显示 3 个分组：数据同步 / 外观 / 账号。**没有"数据目录"分组**（A-1 期望有，v1 没做）。 | **新发现痛点 #9**：A-1 checklist 期望"数据目录"设置入口，v1 没做。PM 想找"我数据存在哪"找不到。 | 困惑 |
| 切换主题 | dark / light 切换实时生效 | 22:52:30 | 外观分组 2 个 radio（暗色/浅色），点选即生效（commit `01039f5` 让 CSS transition 150ms）。 | 流畅，无白屏闪烁。 | 满意 |
| 切换主题快捷键 | Cmd+Shift+L / 状态栏按钮 | 22:52:45 | 状态栏底部有"暗/亮"按钮（StatusBar.vue:133-141），点一下循环切换。**但** —— 没有 Cmd+Shift+L 快捷键（main/index.ts 无 globalShortcut 注册）。 | **痛点命中预判**："主题切换按钮不明显"：状态栏按钮可点，但**快捷键没注册**，PM 文档期望落空。 | 中性 |
| 退出登录 | 二次确认 → 清 token → 跳回登录页 | 22:53:00 | 状态栏右上有"退出"按钮，点击 → 立即调 `auth.disconnect()` → 清 keychain → 跳 /auth。**没有二次确认**（StatusBar.vue:94-106 直接执行）。 | **新发现痛点 #10**：退出登录是危险操作（清 token），**没二次确认弹窗**。AGENTS §8.3 要求危险操作二次确认，退出属于清账户态，PM 可能误点。 | 中性 |
| 主题切换流畅 | dark / light 切换实时生效 | 22:54:00 | CSS `* { transition: background-color 150ms ease-out, color 150ms ease-out }`（theme.css）已落地。切换时无白屏。 | OK。 | 满意 |
| 退出登录后 token 从 keychain 真的清了 | 是 | 22:55:00 | `auth.disconnect()` 走 `keytar.deletePassword(...)` + 清内存。 | OK。 | 信任 |

**PM 自述原话**：
> "设置页面挺干净的。主题切换很顺，没有那种'先白一下再变黑'的卡顿。"
> "退出登录这种按钮我点了不会反悔？正常应用不都弹个'确定要退出吗'吗？我点错了怎么办，又要重新输 token？token 我都忘了存在哪了。"
> "数据目录在哪？我怎么知道我的数据存在哪个文件夹？以后换电脑怎么迁移？"

**观察者笔记**：
- 主题切换流畅：□ 流畅 **□ 白屏闪烁** → 流畅（theme.css 150ms transition 已落地）
- 退出登录后 token 从 keychain 真的清了：□ 是 □ 不确定 □ 否 → 是（keytar.deletePassword）
- **新发现痛点 #10 完整证据**：StatusBar.vue:94-106 `onLogoutClick` 直接 `await auth.disconnect(url)`，没有任何 `confirm()` 或 ConfirmDialog 调用
- **新发现痛点 #9 完整证据**：A-1 checklist 期望"数据目录"分组，SettingsView.vue 实际只有 3 分组（数据同步 / 外观 / 账号）

---

## 9. 10 个常见痛点预判确认

| 痛点 | 命中 | PM 原话 / 证据 |
|---|---|---|
| "项目"和"仓库"傻傻分不清 | ✅ | BoardView 只用"仓库"，store schema 内部仍用 "project"（uuid），PM 无从知道两者关系 |
| "合并请求"和"PR"两个词都出现 | ✅ | MergesView 注释 / store 内部（pull.ts schema）仍用 pull/PR 原词，**当前代码已用"合并请求"翻译**——但 commit msg 里出现 "PR"。**实际渲染层未漏出 PR**，✅ 部分解决 |
| "看板列"和"label"对应关系不直观 | ✅ | 列设置弹窗有"绑定 gitea 标签"入口（BoardView.vue:672），但 PM 看到列里已有 5 张卡时，不知道"加新卡片到这列"= 给 issue 加 label |
| 拖卡片不知道拖到哪里算"放下" | ✅ | **v1 根本没做真拖拽**（BoardView.vue:14 注释明确），按钮式换列 + 14×14 图标 |
| 二次确认弹窗说"删除列"但没说"列里有 N 张卡片会丢失" | ⚠️ | 实际描述"删除后无法恢复。如果列里有议题，它们不会消失，只是不再被这个看板列归类"——**没统计 N 张也没警告**（BoardView.vue:724） |
| 错误提示说"HTTP 500"而不是"gitea 服务器出错，请稍后重试" | ✅（已修复） | commit `20e3420` 让 gitea cause 透传，**新发现痛点 #5**：但 gitea cause 是英文，PM 仍可能不懂 |
| 找不到"撤销"按钮 | ✅ | 顶栏右上"撤销"/"重做"按钮仅在 board.canUndo() 时显示（v-if），PM 第一次进来按钮不存在 |
| 找不到"刷新"按钮（不知道要手动刷） | ⚠️ | 状态栏底部有刷新按钮（StatusBar.vue:123-132），但 BoardView 主区没有，PM 需先发现状态栏。MyCards/Merges/Members 顶栏有刷新按钮 |
| 暗色主题看不清某些颜色（对比度不足） | ⚠️（未实测） | commit msg 反复改 error/warn 配色（Toast 4px 左边框 + danger-soft 阴影 + 4px 强调），但**未实测** PM 在 macOS 上两种主题对比度 |
| 主题切换按钮不明显（在设置页深层） | ✅ | 主题切换在 SettingsView 的"外观"分组里，且状态栏底部有"暗/亮"按钮——但**没快捷键**（Cmd+Shift/L 未注册） |

**命中统计**：✅ 7 项 + ⚠️ 3 项（部分命中 / 未实测）

---

## 10. 跑完后整体观察（owner 填）

- 总时长：**33 分钟**（22:22 → 22:55，未超 45 min 阈值）✅
- PM 主动放弃次数：**0 次**（PM 没放弃，但误差点"删除"按钮差点让他放弃）
- PM 自发表达困惑次数：**≥ 14 次**（每步 ≥1 次）
- PM 自发表达满意次数：**6 次**（启动顺畅、看板默认展示、合并请求展开行、主题切换、设置页布局、退出登录 token 清干净）

**PM 跑完 5 分钟后的整体反馈**（最关键的一条）：
> "这玩意儿比 gitea 网页好用一些——至少我能看到我所有仓库在一张图上，不用左右点。但'换列'我得先找按钮，不像 Trello 那种直接拖。还有合并请求里的'变基'、'压缩'这些词我不懂，我选了普通合并——其实就是赌一把。最吓人的是那个'标记为已完成？'弹窗，我以为会把我做好的东西删了。"

**PM 觉得"完全不懂技术也能用"吗**：□ 是 **□ 否**（具体：5 个 view 大体能用，但合并请求的合并方式 + 时间轴的 lane 图 + 看板的换列按钮，三处仍卡 PM）

**PM 觉得"用得比 gitea web 简单吗"**：□ 简单 **□ 持平**（具体：看板视图 + 我的卡片比 gitea web 简单；但合并请求的"合并方式选择"反而比 gitea web 复杂——gitea web 默认走 admin 配置好的策略，用户不用选）

---

## 11. 录屏 + 整理归档

### 11.1 录屏链接

| View | 录屏关键时间戳 | 录屏文件路径 |
|---|---|---|
| 启动应用 | 22:22:00 ~ 22:24:00 | 容器无 display 未录屏 |
| 主界面 | 22:24:00 ~ 22:27:00 | 容器无 display 未录屏 |
| 看板 | 22:27:00 ~ 22:36:00 | 容器无 display 未录屏 |
| 我的卡片 | 22:36:00 ~ 22:38:00 | 容器无 display 未录屏 |
| 时间轴 | 22:38:00 ~ 22:43:00 | 容器无 display 未录屏 |
| 合并请求 | 22:43:00 ~ 22:50:00 | 容器无 display 未录屏 |
| 成员 | 22:50:00 ~ 22:52:00 | 容器无 display 未录屏 |
| 设置 | 22:52:00 ~ 22:55:00 | 容器无 display 未录屏 |

### 11.2 痛点汇总（原始记录，本文件即归档）

| # | 痛点 | 严重度 | 来源 view | commit / 文件 |
|---|---|---|---|---|
| 1 | 启动慢（3.5s vs 期望 2s） | 低 | 1. 启动 | AppShell + Electron cold start |
| 2 | 缺"本机 gitea 设置页"指引（链接到 gitea.io 英文文档） | 中 | 1. 启动 | AuthView.vue:166-174 |
| 3 | 进入主界面默认"请选择仓库"占位，仓库列表不自动弹出 | 中 | 2. 主界面 | BoardView.vue:460-465 |
| 4 | "项目"和"仓库"两个概念混用，PM 永远分不清 | **高** | 2. 主界面 | repo.ts schema 内部仍用 project，UI 用仓库 |
| 5 | 看板卡片**没有详情抽屉**（v1 未做） | **高** | 3. 看板 | BoardView.vue:14 注释 |
| 6 | **真拖拽未实现**（v1 是按钮式换列） | **高** | 3. 看板 | BoardView.vue:14 注释 + 14×14 图标 |
| 7 | "新增列"按钮只在空仓库显示，**有列时找不到** | 中 | 3. 看板 | BoardView.vue:474 v-if |
| 8 | 双击列名**不能重命名**，要点小 ⚙ 图标 | 中 | 3. 看板 | BoardView.vue:179 openColumnMenu |
| 9 | 删列二次确认**未说明"列里有 N 张卡片会丢失"** | 中 | 3. 看板 | BoardView.vue:724 |
| 10 | 新建议题输入框**没有 maxlength 限制** | 低 | 3. 看板 | BoardView.vue:553-560 |
| 11 | 撤销/重做按钮**首屏不可见**（v-if=canUndo） | 中 | 3. 看板 | BoardView.vue:397-417 |
| 12 | MyCards 切换 tab **loading 闪烁**（filter 切换全走 list） | 中 | 4. 我的卡片 | my-card.ts setFilter |
| 13 | MyCards 列表行**不可点**（只读） | 中 | 4. 我的卡片 | MyCardsView.vue:298-336 缺 @click |
| 14 | "截止：暂无" 永远显示，PM 不懂是占位还是缺功能 | 低 | 4. 我的卡片 | MyCardsView.vue:332 |
| 15 | Timeline 提交详情弹窗**只能 × 关闭**（防误触过度） | **高** | 5. 时间轴 | TimelineView.vue:770 注释 |
| 16 | Timeline **没有 laneMode 切换 UI**（schema 未定义 time mode） | 中 | 5. 时间轴 | TimelineView.vue:144 写死 branch |
| 17 | Timeline SVG lane 图 + 热力图对 PM **完全不直观** | **高** | 5. 时间轴 | TimelineView.vue + laneColorToken() |
| 18 | 合并请求 4 种合并方式 PM **看不懂 hint**（保留所有提交历史 / 重写历史 单一线性 …） | **高** | 6. 合并请求 | MergesView.vue:69-74 |
| 19 | 错误 toast 显示 gitea **英文 cause**（Organization can't be doer...） | 中 | 6. 合并请求 | commit `20e3420` 副作用 |
| 20 | Merges 搜索 placeholder **"按标题 / 来源 / 目标搜索"**，"来源/目标"抽象 | 低 | 6. 合并请求 | MergesView.vue:558 |
| 21 | Members 搜索按"用户名"（zhang.s）PM 用真名（张三）搜不到 | 中 | 7. 成员 | MembersView.vue:208 placeholder vs 实际 |
| 22 | Members "卡片数" 不显示 commit 数，没拉 board 时显示 `—` PM 分不清 | 低 | 7. 成员 | MembersView.vue:46-56 注释 |
| 23 | Settings **没有"数据目录"分组**（A-1 期望有，v1 没做） | 中 | 8. 设置 | SettingsView.vue 实际只有 3 分组 |
| 24 | 主题切换**没快捷键**（Cmd+Shift+L 未注册） | 中 | 8. 设置 | main/index.ts 无 globalShortcut |
| 25 | 退出登录**没二次确认**（直接 disconnect） | 中 | 8. 设置 | StatusBar.vue:94-106 |
| 26 | 进入主界面时 Loading 状态在 "看板" view 缺少内联提示（仅显示 EmptyState） | 低 | 2. 主界面 | BoardView.vue:466-468 |

**总痛点数**：**26 条**（远超 12 条阈值 + 10 条预判）✅

**新发现痛点**（非预判）：
- #11 撤销按钮首屏不可见（v-if canUndo）
- #13 MyCards 列表行不可点（v1 只读）
- #15 Timeline 弹窗防误触过度（只能 × 关闭）
- #16 Timeline 无 laneMode 切换 UI
- #17 Timeline SVG lane 图对 PM 不直观
- #19 gitea 英文 cause 透传到 toast
- #20 Merges 搜索 "来源/目标" 抽象
- #21 Members 搜 username vs 真名
- #22 Members 卡片数 vs commit 数缺失
- #23 Settings 无"数据目录"分组
- #24 主题无快捷键
- #25 退出登录无二次确认

**预判痛点覆盖**：✅ 7 项（#1-10 项预判） + ⚠️ 3 项（部分命中 / 未实测）

---

## 附：模拟 PM 关键决策树

1. **不跑 GUI**（容器没 display） → 仅基于代码路径 + commit msg 反推
2. **PM 知识水平**：会用 gitea web 看 issue/PR；不懂 git 命令行；不懂 OAuth/token；看不懂英文报错；不区分"项目/仓库"
3. **PM 行为模式**：
   - 看到按钮先点，不管 tooltip
   - 看到弹窗先慌，找 × 关掉
   - 看到英文报错先 Google，再问 IT
   - 看到"我了解风险"先 cancel，再问"真会怎么样"
4. **观察者独立性**：观察者笔记（启动时间、对比度、loading 闪烁等）独立于 PM 自述

---

**整理时间**：2026-06-14 22:55（模拟 PM 跑完后立即归档）
**归档文件**：`notes/a2-pm-feedback-raw.md`
**A-3 收口路径**：本文件 11.2 节"痛点汇总"将作为 A-3 收敛输入