// 渲染端占位（M0 脚手架）
// 真实 React UI 由 frontend agent 在 Plan 2 起补全（02-architecture.md §8.5 task 8）
console.log('gitea-kanban renderer (M0 placeholder) ready');
console.log('window.api:', typeof window.api);
